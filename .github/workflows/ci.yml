# Copyright 2026 Jeffrey B. Stewart
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      # go test / go vet / go-licenses fail on a module with no packages,
      # so they are guarded until the first package migrates in (PR 2).
      - name: Test
        run: |
          if [ -n "$(git ls-files '*.go')" ]; then
            go test ./...
          else
            echo "no Go packages yet — skipping"
          fi

      - name: gofmt
        run: |
          dirty=$(gofmt -l .)
          if [ -n "$dirty" ]; then
            echo "gofmt needed on:"
            echo "$dirty"
            exit 1
          fi

      - name: Vet
        run: |
          if [ -n "$(git ls-files '*.go')" ]; then
            go vet ./...
          else
            echo "no Go packages yet — skipping"
          fi

      - name: License check (deny copyleft)
        run: |
          if [ -n "$(git ls-files '*.go')" ]; then
            go install github.com/google/go-licenses@latest
            go-licenses check ./... --disallowed_types=forbidden,restricted
            # restricted already covers the GPL family; belt-and-suspenders
            # for GPLv2-only, which must never appear:
            if go-licenses report ./... 2>/dev/null | grep -E 'GPL-2\.0-only'; then
              echo "GPL-2.0-only dependency found"
              exit 1
            fi
          else
            echo "no Go packages yet — skipping"
          fi

      # Same scan the local pre-commit hook runs, applied server-side to the
      # whole PR diff so a bypassed hook can't slip secrets through.
      - name: Secret scan (PR diff)
        if: github.event_name == 'pull_request'
        run: |
          git fetch origin "${{ github.base_ref }}"
          git diff "origin/${{ github.base_ref }}...HEAD" | ./scripts/presubmit-check.sh

      # The committed topology must satisfy the containment invariants on
      # every PR: the cell's scholar/read-path rules and the inference
      # stack's agency-fronts-infer rules.
      - name: Compose lint (cell + inference containment)
        run: go run ./cmd/compose-lint docker/ai-workers.yaml docker/inference.yaml

      # Header presence and third-party observation repo-wide; year currency
      # on the files the PR touches (main pushes re-verify presence only).
      - name: Copyright headers
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" | go run ./cmd/copyright-lint -
          else
            go run ./cmd/copyright-lint
          fi
